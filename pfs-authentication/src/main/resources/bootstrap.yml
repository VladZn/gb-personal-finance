spring:
  application:
    name: authentication
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:8888
      discovery:
        enabled: true
        serviceId: config
  main:
    allow-bean-definition-overriding: true